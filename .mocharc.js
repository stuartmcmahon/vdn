module.exports = {
  "timeout": 20000, // Allow slow tests (20 seconds).
  "slow": 0, // Force output of (most) times.
  "recursive": "true", // Run tests in subdirs.
  // "colors": true, // Force colored output.
}
